# homebrew-tap

Shared Homebrew tap for Alex Styl tools.

## Install

```bash
brew tap alexstyl/tap
brew install --cask avdbuddy
```

Or directly:

```bash
brew install --cask alexstyl/tap/avdbuddy
```
